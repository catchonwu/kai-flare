{
  "name": "solilop-mvp",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types",
    "db:init": "wrangler d1 execute solilop-db --file=./schema.sql",
    "db:local": "wrangler d1 execute solilop-db --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.5.9"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240821.1",
    "wrangler": "^3.76.0",
    "typescript": "^5.3.3"
  }
}
